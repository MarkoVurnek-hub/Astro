import { Stars } from "drei";
export default () => {
  return (
    <Stars
      radius={300}
      depth={40}
      count={56000}
      factor={4}
      saturation={0}
      fade={true}
    />
  );
};
